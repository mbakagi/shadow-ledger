---
kind: external_dependency
name: Google Fonts — Inter typeface
slug: google-fonts-inter
category: external_dependency
category_hints:
    - client_constraint
scope:
    - '**'
---

Preconnected and loaded from `fonts.googleapis.com` / `fonts.gstatic.com`; declared as the `sans` font family in Tailwind's theme. Requires `font-src https://fonts.gstatic.com` and `style-src https://fonts.googleapis.com` in the CSP meta tag.